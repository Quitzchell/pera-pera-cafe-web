services:
  app:
    build:
      context: .
      dockerfile: ./dev.Dockerfile
    container_name: pera-pera-cafe-app
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
